import { dev } from '$app/environment'

export const title = 'PR WAVE'
export const description = 'SvelteKit blog for PR WAVE'
export const url = dev ? 'http://localhost:5173/' : 'https://prwave.netlify.app/'
